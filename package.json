{
  "name": "austin-ai-club",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "generate:wiki": "node scripts/generate-wiki-manifest.mjs",
    "sync:events": "node scripts/sync-events.mjs",
    "lint:wiki": "node scripts/lint-wiki.mjs",
    "dev": "npm run sync:events && npm run generate:wiki && vite",
    "build": "npm run sync:events && npm run generate:wiki && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "force-graph": "^1.51.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.1.3",
    "vitest": "^4.1.5"
  }
}
