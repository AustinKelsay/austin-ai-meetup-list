{
  "name": "austin-ai-meetup-list",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:events": "node scripts/sync-events.mjs",
    "dev": "npm run sync:events && vite",
    "build": "npm run sync:events && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.1.3"
  }
}
